@using System
@using System.Linq
@model Movie

@functions {
    public IEnumerable<string> GetGenres(string genreString)
    {
        if (string.IsNullOrWhiteSpace(genreString)) yield break;

        foreach (var genre in genreString
            .Split(',', StringSplitOptions.RemoveEmptyEntries)
            .Select(g => g.Trim())
            .Where(g => !string.IsNullOrWhiteSpace(g)))
        {
            yield return genre;
        }
    }
}

<div class="col-12 col-md-6 col-lg-3">
    <div class="card h-100 shadow-sm movie-card">
        <div class="movie-poster-wrapper">
            <img src="@(string.IsNullOrWhiteSpace(Model.PosterUrl) ? "https://via.placeholder.com/500x750?text=No+Poster" : Model.PosterUrl)"
                 class="img-fluid movie-poster"
                 alt="@Model.Title poster"
                 onerror="this.onerror=null;this.src='https://via.placeholder.com/500x750?text=No+Poster';" />

            <div class="movie-overlay">
                <div class="movie-overlay-content">
                    <div class="d-flex align-items-center justify-content-start gap-2 movie-year">
                        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-calendar-event" viewBox="0 0 16 16">
                            <path d="M11 6.5a.5.5 0 0 1 .5-.5h1a.5.5 0 0 1 .5.5v1a.5.5 0 0 1-.5.5h-1a.5.5 0 0 1-.5-.5z" />
                            <path d="M3.5 0a.5.5 0 0 1 .5.5V1h8V.5a.5.5 0 0 1 1 0V1h1.5A1.5 1.5 0 0 1 16 2.5v11A1.5 1.5 0 0 1 14.5 15h-13A1.5 1.5 0 0 1 0 13.5v-11A1.5 1.5 0 0 1 1.5 1H3V.5a.5.5 0 0 1 .5-.5M1.5 2a.5.5 0 0 0-.5.5V4h14V2.5a.5.5 0 0 0-.5-.5zM15 5H1v8.5a.5.5 0 0 0 .5.5h13a.5.5 0 0 0 .5-.5z" />
                        </svg>
                        <span class="fw-semibold">@Model.Year</span>
                    </div>

                    <div class="d-flex flex-wrap gap-2 align-items-center movie-meta">
                        @foreach (var genre in GetGenres(Model.Genre))
                        {
                            <span class="badge genre-badge px-3 py-2 rounded-pill shadow-sm">@genre</span>
                        }
                    </div>
                </div>
            </div>
        </div>
        <div class="card-body d-flex flex-column movie-card-body">
            <div class="d-flex align-items-start justify-content-between gap-3 movie-card-header">
                <h5 class="card-title mb-0 flex-grow-1 movie-title">@Model.Title</h5>
            </div>

            <a asp-controller="Movies"
               asp-action="Details_user"
               asp-route-id="@Model.Id"
               class="btn btn-primary btn-sm mt-auto align-self-center view-details-btn">
                View details
            </a>
        </div>
    </div>
</div>
